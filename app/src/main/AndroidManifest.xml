<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cobeosijek.articlesapp">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.article_list.ArticlesView"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".ui.new_article.NewArticleView"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.article_detail.ArticleDetailView"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.article_edit.EditArticleView"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.on_board.StartingActivity"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>